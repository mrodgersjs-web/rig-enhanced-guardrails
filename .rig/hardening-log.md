# L10 Hardening Log

This file is append-only. Every time `.rig/smoke.sh` discovers a scenario
that fails, it appends a new regression case here. The next revision of
`smoke.sh` is expected to fold that case into a permanent, named scenario
so the test surface only ever grows.

No regressions recorded yet.
